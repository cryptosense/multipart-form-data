(alias
  (name runtest)
  (deps (:tests tests.exe))
  (action (run %{tests})))

(executable
  (name tests)
  (libraries
    multipart-form-data
    alcotest
    lwt.unix
    )
  (preprocess
    (pps
      lwt_ppx
    )))
